# ── Node.js / sidecar ────────────────────────────────────────────────────────
node_modules/
sidecar/tijori-finance-mcp/output/session.json   # Tijori auth session — never commit
sidecar/tijori-finance-mcp/output/               # discovered endpoints + session cache
sidecar/kite_session.json                        # Kite access token — never commit
.env

# ── Julia ─────────────────────────────────────────────────────────────────────
Manifest.toml
*.jl.cov
*.jl.mem
/coverage/
.julia/

# ── Editors ───────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*~

# ── Generated data ────────────────────────────────────────────────────────────
website/data/            # generated by scripts/generate_nse_list.jl — not committed

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
